import type { TsidebarNav } from "../types/ui"

export const authPageRedirectOffer = {
    In: 'Not a member yet?',
    Up: 'Already have an Account?'
}

export const sidebarNav: TsidebarNav = {
    dashboards: ["overview", "servers", "notifiers"],
    pages: ['docs']
}

export const serverPageTabs = ['Info', 'Notifiers', 'Logs']